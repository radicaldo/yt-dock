# Optional: Override host port for the yt-dock UI / API. The container always listens on 8000
# internally; this only changes the port published on your machine.
# Copy this file to .env and edit if 8000 is already in use.
YTDOCK_PORT=8000
# The port will be dynamic if this env file is not used.